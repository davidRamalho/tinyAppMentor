<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
        
      <% if (!username) { %>
      <form method="POST" class="form-inline" action="/login">
          <input name="username" class="form-control" placeholder="Username" type="text" style="width: 300px; margin-right: 1em">
          <button class="btn btn-primary">Sign In</button>
      </form>
      <% } else { %>
        <div class="nav-item nav-link">
            Signed in as: <%= username.email %>
        </div>
        <form method="POST" class="form-inline" action="/logout">
          <button class="btn btn-secondary">Log Out</button>
        </form>
      <% } %>
    </div>
  </nav>
</header>